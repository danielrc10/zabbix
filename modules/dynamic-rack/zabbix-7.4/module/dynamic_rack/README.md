# Módulo dynamic_rack

Diretório pronto para ser copiado para `<frontend-zabbix>/modules/dynamic_rack`.

Consulte a documentação completa em `../../README.md`. Não edite `manifest.json` para alterar configurações
de um rack; todas as opções ficam no formulário do widget no dashboard.
